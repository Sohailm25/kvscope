# ABOUTME: Pinned local runtime dependencies for the first live KVScope serving slice.
# ABOUTME: Keep this file small and aligned with the actual implementation surface.

mcp==1.26.0
modal==1.3.5
openai==2.26.0
PyYAML==6.0.3
requests==2.32.5
transformers==4.51.3
